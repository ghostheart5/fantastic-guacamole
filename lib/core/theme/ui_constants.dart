class UI {
  // ------------------------------------------------------------------
  // Icon sizes
  // ------------------------------------------------------------------
  static const double iconSm = 20.0;
  static const double iconMd = 24.0;
  static const double iconLg = 48.0;

  // ------------------------------------------------------------------
  // Animation durations
  // ------------------------------------------------------------------
  static const Duration durationFast = Duration(milliseconds: 110);
  static const Duration durationMedium = Duration(milliseconds: 250);
  static const Duration durationSlow = Duration(seconds: 3);
  static const Duration snackBarDuration = Duration(seconds: 2);

  // ------------------------------------------------------------------
  // Opacity
  // ------------------------------------------------------------------
  static const double opacityLow = 0.1;
  static const double opacityMid = 0.4;
  static const double opacityHigh = 0.9;

  // ------------------------------------------------------------------
  // Shadow / glow
  // ------------------------------------------------------------------
  static const double shadowBlur = 20.0;
  static const double shadowSpread = 1.0;

  // ------------------------------------------------------------------
  // Interaction
  // ------------------------------------------------------------------
  static const double scalePressed = 0.95;
  static const double borderWidth = 1.0;
}
