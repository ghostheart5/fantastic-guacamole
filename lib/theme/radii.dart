import 'package:flutter/material.dart';

const double r8 = 8;
const double r12 = 8;
const double r16 = 8;
const double r24 = 8;
const double r32 = 8;

const BorderRadius cardRadius = BorderRadius.all(Radius.circular(r8));
const BorderRadius panelRadius = BorderRadius.all(Radius.circular(r8));
const BorderRadius inputRadius = BorderRadius.all(Radius.circular(r8));
const BorderRadius buttonRadius = BorderRadius.all(Radius.circular(r8));
