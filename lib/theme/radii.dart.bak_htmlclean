import 'package:flutter/material.dart';

const double r8 = 8;
const double r12 = 12;
const double r16 = 16;
const double r24 = 24;
const double r32 = 32;

const BorderRadius cardRadius = BorderRadius.all(Radius.circular(r24));
const BorderRadius panelRadius = BorderRadius.all(Radius.circular(r32));
const BorderRadius inputRadius = BorderRadius.all(Radius.circular(r16));
const BorderRadius buttonRadius = BorderRadius.all(Radius.circular(r24));
