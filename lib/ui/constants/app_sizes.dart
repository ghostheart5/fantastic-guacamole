class AppSizes {
  static const padding = 16.0;
  static const radius = 14.0;

  static const xs = 6.0;
  static const sm = 10.0;
  static const md = 16.0;
  static const panelRadius = 22.0;
  static const touchTarget = 48.0;
  static const spaceLg = 24.0;
  static const spaceXl = 32.0;

  // Font-size tier for the small/body text range that is duplicated across
  // many screens (Phase 6, M-1). Values match the literals already in use —
  // this is a naming pass, not a new design scale. One-off hero/display
  // sizes (brand wordmarks, big stat numbers) intentionally stay local.
  static const fontMicro = 8.0;
  static const fontXs = 9.0;
  static const fontSm = 10.0;
  static const fontCaption = 11.0;
  static const fontBody = 12.0;
  static const fontBodyLg = 13.0;
  static const fontLabel = 14.0;
  static const fontLabelLg = 15.0;
  static const fontTitle = 16.0;
}
