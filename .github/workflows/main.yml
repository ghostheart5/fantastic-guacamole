name: Deploy Flutter Web to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Flutter pub get
        run: flutter pub get

      - name: Run security secret guard
        shell: pwsh
        run: ./scripts/security_secret_guard.ps1

      - name: Build web
        run: flutter build web --release --base-href /fantastic-guacamole/

      - name: Copy legal pages to build
        run: |
          mkdir -p build/web/privacy build/web/terms build/web/support build/web/delete-account
          cp assets/legal/privacy_policy.html build/web/privacy/index.html
          cp assets/legal/privacy_policy.html build/web/privacy-policy.html
          cp assets/legal/terms_of_service.html build/web/terms/index.html
          cp assets/legal/support.html build/web/support/index.html
          cp assets/legal/delete_account.html build/web/delete-account/index.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
