# Release audit repairs — September 9, 2026

This is the repair record for findings F1–F14 in `RELEASE_READINESS_AUDIT_C2D83BC0_20260908.md`. Local source, configuration, tooling and disclosure changes address all fourteen findings, with the executed host and Android results recorded below. The original audit and its failing diagnostic evidence remain unchanged. Source repairs do not establish deployed behavior or release acceptance; outstanding platform, backend, signing, public-page and exact-release requirements remain explicit.

## Source and scope

- Repository: `ghostheart5/fantastic-guacamole`; workspace `ChronoSpark-app-only-priority2`.
- Branch: `fix/aab-prebuild-cleanup-20260905`.
- Base commit: `c2d83bc0dd0c7b512e2d2b152a7ca2a230c73b30`; these repairs are uncommitted.
- Prepared version: **4.1.0+2026083022**, synchronized in pubspec and Android properties. This exceeds the known historical 3021 artifact; an unused version above the live Console maximum has not been independently confirmed.
- Frozen app validation snapshot: `C:\src\cs-repairs-0909`. Its 1,954-file source inventory and SHA-256 values are in `test-results/release-repairs-20260909/source-snapshot.json`; aggregate SHA-256 `2a49218b1f780a35b67b8d18a3e71c82a8a93ddbcfda46fe60a910f803db2f5a`. This is an explicitly dirty test snapshot, not a clean committed production candidate. After the full host run and APK build, only six Maestro YAML files, one additional onboarding test and historical wording in the Data-safety draft changed; app, backend and build source remain identical. Only the six YAML changes were copied to the Android test snapshot. `E/maestro-complete-repaired-run.json` records the six-file test delta for the final passing complete replay, including the separate lifecycle-readback flow. The refreshed `E/source-delta-verification.json`, captured at 09:42:48 UTC, confirms eight changed root files, six changed snapshot files and unchanged app/backend/build source in both locations.
- Existing dirty level-20/graphics documents and release artifacts are preserved. No commit, push, cloud deployment, live migration, public-page publication or store upload has been performed during these repairs.
- Test scope excludes monkey testing, level-20 endurance and mutation of the real Moto profile.

Evidence root below: **E = `test-results/release-repairs-20260909/`**. Individual manifests record executable, arguments, timeout, terminal outcome, duration and counts. Overlapping targeted counts must not be added together. The final cross-check of source identity, APK hash, terminal suite counts, unstaged Git state and stopped test runtimes is recorded in E/final-local-verification.json.

Local environment: Windows/PowerShell 5.1, Flutter 3.44.6, Dart 3.12.2, Microsoft JDK 17.0.20.1, Maestro 2.8.0, and the owned `ChronoSpark_Audit_API36` Google ATD x86_64 emulator (`emulator-5580`). The emulator uses Android 16/API36, 1536 MB RAM, software graphics and density160. It is a disposable QA device without the Google Play purchase environment. Builds and runtime suites were sequenced under host memory pressure; the initial integration compile took approximately seven minutes, which is build time, not app startup time.

Android teardown is verified in `E/final-emulator-cleanup.json`: font scale restored to 1.0, viewport overrides reset, and the owned emulator stopped. The initial shutdown script returned exit 1 because ADB briefly still listed the shutting-down emulator; subsequent independent process and ADB readbacks confirmed it absent. This records the final observed state without reclassifying the initial command as successful. The Moto profile, global ADB service, unrelated applications and retained evidence were preserved.

After the bounded Docker availability check described below, the test-started Docker runtime was stopped. `E/docker-cleanup-readback.json`, captured at 09:42:33 UTC, records stop exit 0, status Not running, no Docker processes, no residual vmmemWSL process and no running WSL distributions. Free host RAM returned to approximately 3.25 GiB. No application containers or images were created; no user applications were closed and no global WSL settings were changed. Runtime shutdown does not imply deletion of Docker's initialized on-disk WSL data.

## Finding closure

| Finding | Repaired behavior | Executed evidence / remaining acceptance |
| --- | --- | --- |
| **F1 — Goal account isolation** | Capture account namespace and authentication generation for create/update/complete. Fence supporting reminders, history, Timeline, XP and subsequent planner/UI work. Fence stale profile/log hydration. Serialize daily reminders with account cleanup and suppress stale schedules. | Goal targeted suites pass, including delayed real controller operations and A → B → A. Combined host/device checks recorded below. |
| **F2 — Candidate identity/build readiness** | Prepare 3022 in both authoritative version files and the candidate minimum. Builders discover external signing paths. Existing repository + production-environment build setting names were read back. Candidate preflight now rejects old backend acceptance receipts before materializing signing files. | `E/build-input-readiness.json`; 21 candidate contracts and 15 backend/preflight contracts pass. A fresh signed AAB, live unused-version readback and exact-release device acceptance remain pending. Presence of a secret name does not prove its value or deployed compatibility. |
| **F3 — Trusted AI cohort** | Both AI handlers enforce the server-owned reviewed account digest set before quote/replay/reserve/provider work. Missing or invalid configuration denies access. A safe GET probe supports build-versus-server cohort parity. | 142 Edge tests pass, including handler denial and readiness probes. Deploy the reviewed server cohort and both functions, then run the prepared read-only preflight. No provider call or live debit was used as local proof. |
| **F4 — Goal corruption preservation** | Reject mixed/invalid collections as unavailable, preserve original bytes before replacement, block writes if quarantine fails, and retain previous backups. Bulk writes perform the same read-health check. | Repository regressions cover malformed JSON, mixed records, invalid optional dates, direct bulk save, failed quarantine and later corruption. |
| **F5 — Durable goal save and duplicates** | Return persisted success plus supporting-update warnings. The form closes with accurate saved wording. Canonical save failures still fail. Guard same-frame Save taps and coalesce simultaneous completion calls per account generation/goal. | Targeted tests prove one persisted goal under rapid Save taps and one canonical completion/history/Timeline/40 XP award under concurrent delayed completion. Injected supporting failures do not invite duplicate creation. |
| **F6 — Honest Progression state** | Typed loading/unavailable/empty/ready review, no reassuring fallback ratings when evidence is unavailable, retry without creating work, correct empty-state action, completed work excluded from active recommendations. Real malformed milestone storage is unavailable and preserved; retry clears the cached predictor family. | 37 final Progression/milestone tests pass, including real repository preservation and failed predictor recovery. Earlier 25-test root run is superseded where the follow-up changed source. |
| **F7 — Signed-out deletion status** | Keep the installed client URL. Permit capability status at the gateway while requiring exact-token Auth validation, matching live session and recent login for deletion. Bind both receipt parts and enforce DB-time expiry through the existing service-only RPC. | 40 Flutter auth/gateway tests pass; handler cases are included in the Edge suite. Real isolated SQL tests cover valid, wrong, expired and unknown capabilities. Deploy `account-delete` with only its gateway JWT check disabled, then verify actual 400/404/401 status/mutation rejection behavior. |
| **F8 — Obsolete debit RPC** | Migration `20260909065846_revoke_obsolete_client_credit_debit.sql` removes EXECUTE from PUBLIC, anon, authenticated and service_role. It changes no wallet rows. Canonical service reserve/settle remains authoritative. | Isolated PostgreSQL fixture: nine role/allowance debit attempts denied, balances and ledger unchanged. Exact preflight SQL + Node validator checked 12 privilege records under a read-only transaction. Live migration/grant readback remains pending. |
| **F9 — Disclosures match eligible internal behavior** | Four canonical legal pages, 17 generated copies, current wiki/listing/Data-safety drafts distinguish contained public behavior from private subscription/credit/AI tests and describe consent, providers, usage metadata and Google Play cancellation. | 13 legal/source contracts and generated-copy parity pass. Prices and existing age/retention/support promises are unchanged. Public URL publication/readback and Console declarations remain pending. |
| **F10 — Signing files outside checkout** | Relocated the existing properties and upload keystore to the restricted per-user LocalAppData ChronoSpark signing directory. Verified identical before/after hashes and restricted inherited ACLs. The upload identity was preserved. | `E/signing-relocation.json`, `E/build-input-readiness.json`; secret-location guard now passes. Explicit signing arguments override path environment variables, which override the LocalAppData default. Clean-source/configuration/external-path guards and temporary-key cleanup remain enforced. |
| **F11 — PowerShell Edge contract capture** | Use ProcessStartInfo capture compatible with Windows PowerShell 5.1, preserving terminal exit, stderr and timeout semantics. | Six real Deno contract scenarios pass, including assertion failure and timeout. Local PowerShell 5.1 is proven; CI PowerShell 7 parity is not inferred. |
| **F12 — Small-screen welcome brand** | Fit the brand or intentionally break between CHRONO and SPARK; retain the spoken CHRONOSPARK label and bound only the decorative brand scaling. | Native 320x640 at 100% and 200% font scale passes visual readback. At 200%, a native swipe reveals all paragraph text above the fixed CTA, and a real CTA tap advances First Setup. The added host regression verifies every line can be scrolled into readable position. `E/welcome-scroll-probe.json` and screenshots. |
| **F13 — Creator Maestro selectors** | Match the native required-field hint without removing its accessibility semantics. Reveal review/title/category/library controls beyond clipped or offscreen accessible bounds before tapping. Reveal Timeline navigation after scrolling away from its header. Both lifecycle flows now require traversal to RECENT RAW OBSERVATIONS before centering and asserting the saved completed/helped row. | The earlier 28-file YAML validation and selector contract pass. Six test-only YAML repairs retain all confirmation and persistence assertions. Creator and both repaired lifecycle flows passed their focused native replays. The final uninterrupted canonical eleven-journey replay also passed all eleven flows with zero failures/errors/skips/captured app fatal markers. Prior failed sequences remain preserved below. |
| **F14 — Integration tap validity** | Required controls must be visible and hit-testable; missed taps fail. Close the keyboard before scrolling. Creator asserts exactly one saved task with priority 4 and the selected scheduled date. | Android app-root integration passes at the original 320x640 viewport (nine cases) and the taller 411x891 viewport (six repeated auth/navigation cases). All manifests show terminal success, zero failures/errors/skips. |

## Combined validation

| Check | Result | Evidence and limits |
| --- | --- | --- |
| Formatting and fatal static analysis | PASS | `E/final-prechecks.json`, `final-format.txt`, `final-analysis.txt`. First combined analysis exposed eight goal-lane lint issues; they were repaired, and the final combined analysis is clean. `E/final-post-native-checks.json` additionally passes format/fatal analysis for the only later Dart test change, final diff checks and source-delta verification. |
| Workflow/source release contracts | PASS | 12 workflows, 21 candidate tests, 15 Node preflight/billing tests. `E/final-prechecks.json`. Negative build-entrypoint test proves old successful receipts cannot create signing inputs. |
| Secret location/content, architecture, release/version, golden comparison, coverage contracts, Maestro YAML and legal parity | PASS | `E/static-checks.json` and per-command logs. The initial analyzer entry in that file is superseded by the clean final precheck, not erased. |
| Full Flutter suite with coverage | PASS | `E/flutter-suite-final-manifest.json`: **2,687 passed, zero failures/errors/skips**, terminal success, exit 0, 862.268 seconds. Canonical bounded wrapper: `test --no-pub --coverage --concurrency=1`. File hashes were unchanged throughout the final run. The initial full run's 2,686 passes/one outdated privacy-assertion error remain preserved in `flutter-suite-manifest.json`; the corrected rendered-copy assertion was included in the full rerun. |
| QA configuration and coverage ratchet | PASS | `E/qa-config-manifest.json`: 15 passed, no failures/errors/skips. `E/coverage-final-ratchet-console.txt`: 74% overall, 91.5% critical coverage; required floors pass. Coverage includes 29 absent production files conservatively counted at zero and 64 reviewed declaration-only exclusions. |
| Complete non-build gate twice, including full Supabase replay/RLS/schema lint and pinned platform CI | UNVERIFIED | Required by `docs/RELEASE_TEST_PLAN.md`. The initial 2,686-pass/one-error repair run is not a qualifying pass. A local PostgreSQL fixture is not the complete database workflow. The earlier unavailable-pipe observation in `E/docker-availability.json`/`.txt` is superseded: one bounded startup of installed Docker succeeded in 38.36 seconds (`E/docker-start-attempt.json`), and engine readback passed on 29.7.2 with zero containers and images (`E/docker-runtime-readback.json`). Only approximately 0.78 GiB of host RAM remained free after the empty-engine readback, so the full Supabase stack was not started. Startup initialized Docker's WSL runtime without application upgrades or settings edits; no Supabase image pulls, application containers or SQL mutations were performed. The remaining local limitation is host memory, not persistent engine unavailability. Pinned GitHub CI was not triggered under this repair turn's no-push boundary. Preserve two successful independent exact-source gate receipts before declaring this requirement met. |
| Higher coverage targets | OPEN NONBLOCKING DEBT | `E/coverage-final-target-result.json` and `coverage-final-target-console.txt`, exit 1: domain use cases 82.4% / target 85%, Play paywall 86.6% / 88%, backup 94.5% / 95%, authentication 89% / 90%. These preexisting higher-target gaps remain explicit; the passing ratchet does not close them. Targets were not lowered and no 100% coverage claim is made. |
| Complete Edge gate | PASS | `E/backend/edge-functions-final.junit.xml`: 142 tests, zero failures/errors/skips; entrypoint checks, format and lint passed. Local Deno 2.9.4 differs from CI's pin. |
| Isolated database execution | PASS within fixture | PostgreSQL 18.3; nine actual denied debit calls, four receipt cases and 12 effective privilege rows. This is not a full Supabase stack replay or deployed database proof. Cluster stopped. |
| Repaired QA Android build and identity | PASS | `E/qa-build-checks.json`, `E/qa-apk/identity.json`, `badging.txt`. Canonical `flutter build apk --debug --target-platform=android-x64 --no-pub --dart-define-from-file=tool/qa_defines.json`; retained `E/qa-apk/app-debug.apk`, 96,905,905 bytes, SHA-256 `9f53653c95fd3cba620e287961ee58dce053eaba8630c055b035ef2356090fd4`. Package `com.ghostheart5.chronospark`, 4.1.0/2026083022, min24/target36, debuggable. QA mock authentication is distinct from production billing. |
| Native welcome and post-native host regressions | PASS | `E/welcome-320.png`, `welcome-320-font200.png`, `welcome-320-font200-scroll1.png`, `welcome-320-font200-login.png`, and probe JSONs. Last image is First Setup step 2, not authenticated login. `E/ui/post-native-host-manifest.json`: 24/24 passed, zero failures/errors/skips (11 onboarding, 13 legal/source checks). |
| Maestro journeys on retained QA APK | PASS | `E/maestro-complete-repaired-run.json` and `E/maestro/20260909-041904-c2d83bc0-qa-journeys/manifest.json`: **11/11 flows passed** in one uninterrupted canonical sequence, exit 0, zero failures/errors/skips and no timeout. Manifest duration 1,149.117 seconds (outer wrapper 1,175.05 seconds); logcat capture remained active and recorded zero app fatal markers during that window. The exact tested APK is `E/qa-apk/app-debug.apk`, SHA-256 `9f53653c95fd3cba620e287961ee58dce053eaba8630c055b035ef2356090fd4`, on the disposable API36 ATD. This verifies the defined QA journeys, including subscription containment, not real Google Play purchase lifecycles or global crash freedom. Its six-file test delta and all four earlier canonical failures remain preserved. |
| Android app-root integration | PASS | `E/android-integration-runs.json`: nine cases at 320x640 (startup1, auth/navigation6, persistence1, planner-learning identity1), plus the six auth/navigation cases repeated at 411x891. All five per-file manifests have exit0, terminal success and zero failures/errors/skips. Canonical bounded Flutter test wrapper, 900 seconds per file, `--no-pub -d emulator-5580`. `E/integration-apk-identities.json` records each separately compiled integration APK hash immediately after completion; these test-fake APKs are distinct from the retained QA APK and do not prove Play purchases. |
| Current-source signed internal AAB / installed release / live backend / live Console | UNVERIFIED | Local source fixes and QA results do not establish these requirements. |

### Preserved native failure history

These runs are diagnostic failures, not qualifying passes. Each canonical sequence stopped on its first failure; later unexecuted flows cannot be counted as passed.

- `maestro/20260909-030811-c2d83bc0-qa-journeys`: Planner passed; Creator's review control had native bounds `[36,639][284,640]` and the nominally visible tap did not open confirmation. Fixed the required reveal/centering step.
- `maestro/20260909-031558-c2d83bc0-qa-journeys`: the conditional Start login tap became stale after the actual login form opened. Only that guide-reveal tap is optional now; the following authentication/Nexus waits and all authentication/save/purchase controls remain required.
- `maestro/20260909-033302-c2d83bc0-qa-journeys`: Planner passed and Creator saved the task, then the next title field was below the saved receipt and outside the viewport. Added required visible-control scrolling for title/category/library/linked-goal actions and the analogous later Timeline navigation. The complete Creator-only replay subsequently passed.
- `maestro/20260909-034642-c2d83bc0-qa-journeys`: nine flows passed, including Creator and account isolation. The tenth, learned-lifecycle, failed when its final required scroll did not reveal the saved completed/helped ledger row after restart. The separate eleventh lifecycle-readback flow was not executed. The repair adds required traversal through the expanded Planning & guidance category to RECENT RAW OBSERVATIONS, then centers and asserts the actual saved row in both lifecycle flows. The repaired lifecycle and separate readback subsequently passed their focused replays (`041221` and `041733`), followed by the complete eleven-flow pass in `041904`.

After the fourth failure, an actual native swipe through the page gutter revealed two Task Lifecycle rows showing completed, Completed the task and helped. `E/ledger-scrolled-native.png` and its XML preserve that supplemental visibility/persistence observation. The generic row labels do not independently establish each task's identity, and this manual readback does not turn either automated lifecycle flow into a pass.

The focused precursor receipts also remain available: Creator in `E/maestro-creator-focus-1-run.json` / `maestro/20260909-034111-c2d83bc0-custom/manifest.json`, lifecycle in `E/maestro-lifecycle-scroll-focus-run.json` / `maestro/20260909-041221-c2d83bc0-custom/manifest.json`, and restart readback in `E/maestro-ledger-readback-focus-run.json` / `maestro/20260909-041733-c2d83bc0-custom/manifest.json`. Each is one terminal passed case with exit 0 and zero failures/errors/skips/captured app fatal markers. They overlap the final canonical suite and must not inflate its eleven-flow count.

All four failed runs recorded zero app fatal markers. Earlier receipts, including the historically named `maestro-source-accepted.json` and `maestro-full-final-run.json`, are preserved unchanged; their filenames are not acceptance claims. The final passing complete replay's six-file flow hashes are recorded in `maestro-complete-repaired-run.json` and match the refreshed source-delta readback. Acceptance of the defined QA sequence rests on its separate terminal `041904` manifest, not on combining precursor passes. Maestro 2.8.0 also emitted a Windows heartbeat-file locking error in diagnostic logs; this tooling warning is separate from application fatal-marker scanning and cannot substitute for terminal JUnit results.

## Prepared external completion

The precise backend deployment order, negative probes, compatibility limits and rollback choices are in `E/backend/PREPARED_ROLLOUT.md`:

1. Commit/push the reviewed source and integrate the build-tooling changes into the workflow's allowed tooling branch. Preserve the immutable candidate SHA, reviewed policy hash, rollback source and required independent exact-source gate receipts. The current working branch differs from the workflow's permitted tooling branch; do not silently weaken that provenance check.
2. Verify the intended production project and privately match the server cohort to the reviewed build cohort.
3. Apply only the reviewed credit-revocation migration and verify effective grants.
4. Deploy `ai-proxy`, `planner-explanation` and the secured `account-delete` handler/configuration from the reviewed source. Keep Planner explanation's existing feature rollback in place.
5. Run the new read-only backend repair preflight from that immutable source; test a valid signed-out receipt using an approved disposable deletion fixture before treating the full deletion journey as accepted.
6. Publish the reviewed public legal copies and independently read back the public URLs and relevant Console declarations. The Pages workflow publishes on a push to `main`; a branch push or manual build is not public-page publication proof.
7. Confirm an unused version code above the live maximum, require the successful exact-source CI and deployed-backend evidence, and run the signed candidate workflow.
8. Inspect the produced AAB/signature/manifest/native alignment and present its immutable source, version and artifact hash. Obtain explicit approval against that produced artifact before Google Play internal-track upload and installation. GitHub build/evidence-artifact approval does not authorize a Google Play upload. After that approval, use the internal update route and validate the exact installed candidate while preserving the Moto profile.

**Release decision remains NO-GO while release-critical platform, deployed configuration, signed-artifact and exact-release device requirements are unverified.** Local source repairs and the recorded Android regression journeys are complete. The next release stage is to finish the remaining platform gates, freeze the reviewed candidate and carry out the concrete external completion steps with the required authorization. A passing QA APK is not a signed internal-testing AAB or acceptance of the installed Google Play release.
