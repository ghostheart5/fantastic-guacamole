CHRONOSPARK DATABASE DESIGN UPGRADE PLAN
SAFE EXECUTION TRACK
Date: 2026-07-30

Purpose:
- Execute KEEP + NORMALIZE + REFACTOR safely.
- Define target database architecture.
- Provide phased safe roadmap for database design upgrade.

============================================================
1) EXECUTED SAFE CHANGES (CODE-ONLY, NO SQL)
============================================================

Completed now:
1. Metrics upsert conflict key aligned to user ownership.
   - File: lib/system/analytics/global_aggregation_service.dart
   - Change: onConflict changed from device_id,date to user_id,date
   - Why safe: aligns app write path with hardened user-scoped PK strategy.

2. Monetization table lookup normalized to canonical-first with fallback retained.
   - File: lib/features/monetization/data/repositories/subscription_repository.dart
   - File: lib/features/monetization/data/repositories/ai_credit_repository.dart
   - File: lib/features/monetization/data/repositories/entitlement_repository.dart
   - Change: monetization_* tables are first; legacy alias tables remain as fallback.
   - Why safe: no behavior removal, fewer failing first attempts, less schema ambiguity.

Safety guarantees:
- No migration executed.
- No RLS/policy/function changes.
- No table drops/renames.
- Legacy compatibility preserved in app repositories.

============================================================
2) KEEP (DO NOT REPLACE)
============================================================

Keep as-is:
- Local-first core persistence for tasks/goals/routines/timeline/profile/settings.
- Supabase monetization relational model.
- Supabase storage backup channel (chronospark-sync).
- user_daily_metrics as aggregate telemetry table.

Reason:
- Current system is operational and resilient offline.
- Monetization path is already shaped for user ownership and table-level separation.

============================================================
3) NORMALIZE (SAFE, INCREMENTAL)
============================================================

N1. Table naming normalization
- Canonical names:
  - monetization_subscription_statuses
  - monetization_wallets
  - monetization_credit_transactions
  - monetization_purchases
  - monetization_entitlement_events
- Keep legacy alias fallback temporarily.
- Add telemetry/logging for fallback hits to measure deprecation readiness.

N2. Ownership normalization
- Enforce user-scoped writes/reads at app layer in every future remote table access.
- Keep auth.uid ownership assumption explicit in all repository methods.

N3. Completion semantics normalization
- Canonical events to support later DB fact table:
  - completed
  - skipped
  - not_completed
  - rescheduled
  - overdue

============================================================
4) REFACTOR (SAFE BOUNDARY CUTS)
============================================================

R1. Creator boundary refactor
- Split creation command DTO from storage projection.
- Keep current local write behavior while preparing remote canonical table write adapters.

R2. Timeline boundary refactor
- Keep timeline UI behavior.
- Refactor timeline data source to prefer computed projections from canonical facts once available.

R3. Persistence contract refactor
- Introduce explicit repository contracts for:
  - canonical facts
  - derived projections
  - backup snapshots

============================================================
5) TARGET DATABASE ARCHITECTURE
============================================================

Target model (remote canonical facts):
- profiles (existing)
- tasks
- goals
- habits (canonical replacement for routines naming)
- notes
- completion_events
- monetization_subscription_statuses (existing)
- monetization_wallets (existing)
- monetization_credit_transactions (existing)
- monetization_purchases (existing)
- monetization_entitlement_events (existing)

Target ownership:
- All canonical planner tables include user_id with strict own-row RLS.
- Completion/event tables are append-only where possible.

Target timeline approach:
- Timeline as projection/view over canonical facts + completion events.
- Avoid timeline as sole source of truth.

Target consistency approach:
- Local-first cache remains.
- Canonical remote facts become sync authority for cross-device consistency.

============================================================
6) SAFE DATABASE ROADMAP
============================================================

Phase 0: Guardrails
- Add architecture decision note for canonical table names and ownership rules.
- Add fallback-hit telemetry for legacy alias table paths.

Phase 1: Read path hardening
- Ensure all monetization repositories query canonical first (done).
- Add explicit column projection in Supabase reads where feasible.

Phase 2: Completion event contract
- Add domain event model for completion lifecycle (code-level only first).
- Keep current storage paths unchanged while dual-writing to local event stream.

Phase 3: Canonical planner schema design
- Draft SQL for tasks/goals/habits/notes/completion_events with RLS and indexes.
- Review-only gate before any migration run.

Phase 4: Dual-write rollout (feature-flagged)
- Write to local + remote canonical tables for selected entities.
- Validate parity dashboards before switching reads.

Phase 5: Projection cutover
- Move timeline and analytics reads to canonical projections.
- Keep rollback switch to local-only reads.

Phase 6: Alias deprecation
- Remove legacy alias fallbacks only after zero-hit telemetry window.

============================================================
7) DATABASE DESIGN UPGRADE CHECKLIST
============================================================

Before any SQL migration:
- Verify full schema inventory in deployment env.
- Verify RLS policy matrix for every new table.
- Verify backfill strategy from local snapshots.
- Verify rollback plan and feature flags.

Before read cutover:
- Validate parity between local-derived and remote-derived timeline/completion summaries.
- Validate no cross-user leakage by policy tests.
- Validate performance on high-cardinality completion_events.

Before alias removal:
- Confirm fallback-hit telemetry is zero across stable window.
- Confirm deployed schema includes canonical monetization tables only.

============================================================
8) RISK CONTROL SUMMARY
============================================================

Highest-risk operations (defer until gated):
- RLS policy edits on monetization tables
- security definer function changes
- non-reversible table renames/drops

Low-risk operations (safe now):
- canonical-first table read ordering (completed)
- conflict key alignment with user ownership (completed)
- app-layer architecture docs and contracts

============================================================
9) DEFINITION OF DONE
============================================================

Done for this execution request:
- KEEP executed by preserving stable systems.
- NORMALIZE executed in active monetization query paths.
- REFACTOR executed at risk boundary level (query order + conflict ownership alignment).
- Target architecture defined.
- Safe roadmap defined.
- Database design upgrade plan documented.
