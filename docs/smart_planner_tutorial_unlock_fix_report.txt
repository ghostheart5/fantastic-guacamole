CHRONOSPARK SMART PLANNER TUTORIAL UNLOCK FIX REPORT

Files changed
- lib/features/home/ui/smart_coach_screen.dart
- lib/tutorial/mission/mission_state.dart
- docs/smart_planner_tutorial_unlock_fix_report.txt

Every Smart Planner mission/tutorial call found
- lib/features/home/ui/smart_coach_screen.dart
  - reportSmartCoachQuestionAsked() after initial coaching response
  - reportSmartCoachQuestionAsked() after follow-up response
- lib/tutorial/mission/mission_event_bridge.dart
  - reportSmartPlannerQuestionAsked()
  - reportSmartCoachQuestionAsked()
- lib/tutorial/mission/mission_controller.dart
  - reportSmartPlannerQuestionAsked(MissionState current)
- lib/tutorial/mission/mission_provider.dart
  - reportSmartPlannerQuestionAsked()
  - reportSmartCoachQuestionAsked()

Whether reportSmartCoachQuestionAsked remains
- Yes.
- It remains in the mission bridge/provider for compatibility.
- It is no longer called from SmartCoachScreen during normal Smart Planner use.

Whether Smart Planner is still required for First Setup
- No.
- Smart Planner is not required for First Setup.
- First Setup no longer depends on asking Smart Planner anything.

Old mission sequence
- createFirstGoal
- configureFirstItem
- askSmartPlannerQuestion
- openTimeline
- complete

New mission sequence
- createFirstGoal
- configureFirstItem
- openTimeline
- complete

Compatibility handling for old askSmartPlannerQuestion progress
- Added migration logic in mission_state.dart during MissionState.fromJson.
- If a saved mission state was still active on askSmartPlannerQuestion:
  - that step is marked completed
  - the next non-Smart-Planner setup step is activated
  - openTimeline becomes active if not yet completed
  - complete becomes active if openTimeline was already completed
- This prevents existing users from being trapped on a removed required step.

Confirmation Smart Planner still works as an optional feature
- Smart Planner screen still opens normally.
- Get Insight flow was not changed.
- Follow-up question flow was not changed.
- Crisis detection was not changed.
- Voice output behavior was not changed.
- AI request logic was not changed.
- CoachQueryController was not changed.

Confirmation analytics were preserved
- Preserved analytics events:
  - smart_coach_requested
  - smart_coach_response_rendered
  - smart_coach_followup_requested
  - smart_coach_followup_response_rendered
- No Smart Planner analytics events were removed.

Confirmation auth/Supabase/Firebase/Google/AI/SI Console/subscription/database schema were untouched
- Auth untouched
- Supabase untouched
- Firebase untouched
- Google login untouched
- AI logic untouched
- SI Console logic untouched
- Subscription logic untouched
- Database schema untouched
- No SQL run

Analyzer result
- Command: flutter analyze
- Result: No issues found.
