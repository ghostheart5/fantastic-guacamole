# Uncovered feature areas

The following feature folders currently lack dedicated regression tests and are tracked as intentional audit follow-ups:

- lib/features/analytics/
- lib/features/notifications/
- lib/features/voice/
- lib/features/workbench/
