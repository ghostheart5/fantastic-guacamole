# Apply Verified Purchase Grant Results

Complete this template only from manually captured staging SQL output. Do not record credentials, connection strings, or secrets.

Grant verification date:

Verifier:

Target project:

Function signature:

anon execute:

authenticated execute:

service_role execute:

Result: PASS / FAIL

Blocking findings:

Next action:

Production release status: **NO**