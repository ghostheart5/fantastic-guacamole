# Retired staging-validation archive

Status: **historical evidence only; execution disabled**

The project previously referenced by this archive is not an authorized
ChronoSpark environment. Its identifier has been removed and replaced with a
non-routable marker. All PowerShell and SQL files beneath this directory begin
with a fail-closed guard so an old instruction cannot reach any Supabase
project.

The sole ChronoSpark production authority is:

- Project name: `ghostheart5's Project`
- Project ref: `qpwhuckyirnqtmvhpede`
- Production branch: `main`
- Region: `us-west-2`

Do not replace the retired marker with the production ref. Production
verification must use current, purpose-built, read-only checks where possible;
any mutation requires a reviewed migration or Edge Function deployment with a
dry-run/drift gate and explicit production authorization.

The remaining files are retained only to preserve historical audit context.
Their former pass/fail statements are not current GhostHeart5 evidence.
